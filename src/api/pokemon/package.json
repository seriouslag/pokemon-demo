{
	"name": "@myapi/api-pokemon",
	"version": "0.0.1",
	"description": "Pokémon API",
	"scripts": {
		"downloadApi": "ts-node --project tsconfig.json src/tools/download-api.ts",
		"generateApi": "rm -rf src/generated/* && openapi --input api/api.yaml --output src/generated --useOptions --useUnionTypes --name PokemonClient --client fetch"
	},
	"dependencies": {},
	"devDependencies": {
		"openapi-typescript-codegen": "^0.23.0"
	},
	"peerDependencies": {}
}
